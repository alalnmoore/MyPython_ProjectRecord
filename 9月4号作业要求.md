用户可能会在约会前、聊天后、吵架后记录自己的情绪。请使用 FastAPI 实现一个简单的“情绪记录 API”，用于接收用户提交的情绪记录，并根据情绪给出一条简单建议。

可以参考课堂最后的“用户管理 API”综合练习，需要注意的是，如果我们希望继承了BaseModel的对象在接收参数的时候，有些属性值我们希望可以不传，我们可以这样定义：

class User(BaseModel):
 # 这样一来，这个属性的值在请求体中可以不传，也不会报错
 email: str | None = None
功能要求
1. 根路径接口
请求：

GET /
要求返回欢迎信息，例如：

{
  "message": "欢迎使用情绪记录API"
}
2. 创建情绪记录
请求：

POST /diary
Content-Type: application/json
请求体字段：

字段	类型	是否必填	说明
user_id	int	是	用户ID
content	str	是	情绪记录内容
mood	str	是	情绪关键词，例如开心、焦虑、生气、难过
scene	str	否	发生场景，例如约会前、聊天后、吵架后
要求：

使用 Pydantic 定义请求体模型 DiaryCreate
创建成功后自动生成 diary_id
将记录保存到内存字典中
返回创建后的完整记录
示例请求：

{
  "user_id": 1,
  "content": "今天约会前有点紧张，担心自己不知道聊什么。",
  "mood": "焦虑",
  "scene": "约会前"
}
示例响应：

{
  "diary_id": 1,
  "user_id": 1,
  "content": "今天约会前有点紧张，担心自己不知道聊什么。",
  "mood": "焦虑",
  "scene": "约会前"
}
3. 根据 ID 查询情绪记录
请求：

GET /diary/{diary_id}
要求：

使用路径参数接收 diary_id
如果记录存在，返回记录详情
如果记录不存在，返回：
{
  "error": "情绪记录不存在"
}
4. 查询情绪记录列表
请求：

GET /diaries
支持查询参数：

参数	类型	是否必填	默认值	说明
user_id	int	否	无	按用户ID筛选
mood	str	否	无	按情绪筛选
page	int	否	1	页码
page_size	int	否	10	每页数量
要求返回：

{
  "total": 2,
  "page": 1,
  "page_size": 10,
  "diaries": []
}
代码要求
项目入口文件为 main.py
至少定义两个 Pydantic 模型：
DiaryCreate
DiaryResponse
使用 diaries_db = {} 模拟数据库
使用 next_diary_id 生成自增 ID