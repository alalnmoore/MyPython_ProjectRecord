# 2026-09-09

## GPTimage-2 项目初始化
- 项目目录:`C:\Users\Lenovo\Desktop\python_workplace\MyfastApiProject\GPTimage-2`
- 来源:用户给了一份 rtoc.cc 中转站 API 文档(本地 文档.md),希望调用 `gpt-image-2` 和 `gpt-image-2.5` 做文生图、图生图
- 实际写出的文件:
  - `gpt_image_client.py`:`GPTImageClient` 类,封装文生图 `/v1/images/generations` + 图生图/编辑/遮罩 `/v1/images/edits`,模型名参数化(默认 `gpt-image-2`,可直接传 `gpt-image-2.5`),含 `_demo()` 命令行入口
  - `examples.py`:展示 list_models / 文生图 / 单图生图 / 多图生图 / 遮罩编辑 5 种常见用法
- 设计要点:
  - `image[]` 用 `requests` 的 `files=[("image[]", ...)]` 列表写法,保证多参考图同名字段
  - mask 与多图都用 `files=` 而不是 `data=`,避免被当成普通表单字符串
  - 超时默认 180 秒(文档建议)
  - API Key 支持参数传入或环境变量 `RTOC_API_KEY`
  - 模型名彻底参数化,因为文档中**只列出了 `gpt-image-2`,没有 `gpt-image-2.5`**,先提示用户调用 `/v1/models` 确认
- 文档里 gpt-image-2 的关键约束(都已写在代码注释):
  - 不支持 `background=transparent`、不接受 `input_fidelity`
  - size:两条边都是 16 的倍数、最长边 ≤3840、长短边比例 ≤3:1、总像素 655360~8294400

## 实测记录(用用户提供的 key)
- key 已在对话明文发出,**已提醒用户重置**;代码与 memory 不记录 key 本身
- 资源组实际可用模型(2026-09):
  - `gpt-image-2` ✅ 实测可生成(1024x1024/low/87.4s → 1.9MB PNG)
  - `gpt-image-2.5` ⚠️ 模型名在 `/v1/models` 可见,但调用返回 503 `No available compatible accounts`(中转站当前没渠道)
  - `gpt-image-2.5-flare` / `gpt-image-2.5-sunburst` 在列表里,未实测

## gpt-image-2.5-flare 实测(用户后续要求)
- ✅ 可用,48.4 秒生成 1024x1024/low PNG(1.97MB),比 gpt-image-2 的 87 秒快近一倍
- 保存到 `output\gpt-image-2_5-flare_test.png`
- 推测 `gpt-image-2.5-sunburst` 也是类似轻量变体,渠道可用;但 2.5 主版本(48 秒对应的是 flare 变体,非主版本)仍然 503
- 环境准备:managed Python venv `C:\Users\Lenovo\.workbuddy\binaries\python\envs\default`,已装 `requests`
- Bash 陷阱:Git Bash `/tmp` 路径 Python 解析成 `c:\tmp`,要调用脚本用 `cygpath -w /tmp/x` 拿真实 Windows 路径(默认 `C:\Users\Lenovo\AppData\Local\Temp\`)
- 代码 bug 修复:`list_models()` 之前调了不存在的 `_raise_for_status`,已改为 `_parse_json_response`
- 生成长任务(>120s)必须 `run_in_background=true` + `TaskOutput` 拉结果

## 后续改造:用户提出三个需求
- 创建 `.env`(含真实 key) + `.env.example`(占位) + `.gitignore`(忽略 .env)
- 创建 `refs/` 参考图目录,加 `README.md` 告知用途
- `gpt_image_client.py` 加 `SIZE_PRESETS`(8 档)/ `QUALITY_PRESETS` / `FORMAT_PRESETS` 常量,加 `list_local_images()` 静态方法
- `examples.py` 改成交互式 CLI:`main()` 菜单 + `text_to_image_flow()` / `image_to_image_flow()` / `list_models_flow()`,菜单用 `_pick()` 让用户从预设中选
- venv 多装了 `python-dotenv`,`gpt_image_client.py` 顶部 `try: from dotenv import load_dotenv; load_dotenv()`,缺包时降级
- examples.py 顶部 `os.chdir(HERE)` 确保 dotenv 找到项目根的 `.env`(而不是用户 cwd)
- ⚠️ `.env` 文件虽然比 chat 安全,但仍可能被截图/记录;**仍强烈建议去 rtoc.cc 后台重置 key**

## 进度条功能(用户后续要求:终端加生成图片进度条)
- 装 `rich`(用其 `Console.status` 的 dots12 spinner)
- 在 `gpt_image_client.py` 顶部 `try: from rich.console import Console`,缺包时降级到 ANSI `\033[36m` + unicode frames(`⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏`)
- 新增模块级 `_call_with_progress(label, func, *args, **kwargs)`:
  - 后台线程跑 func,把结果/异常塞进 holder dict
  - 主线程跑 spinner,实时显示 `label + ⏱ {elapsed:.1f}s`
  - 线程结束后清行,主线程拿到结果或 raise 异常
  - `show_progress=False` 可关掉进度条
- `text_to_image` / `image_to_image` 都加了 `show_progress: bool = True` 参数,默认开启
- 实测 gpt-image-2.5-flare 54.9s 生成,spinner 动画正常(只是 Bash 工具捕获 stdout 时 `\r` 同行刷新被折叠,真实终端能看到动画)

## 模型菜单化(用户后续要求)
- `examples.py` 顶部加 `AVAILABLE_MODELS = [(display, value), ...]`,目前三个:`gpt-image-2`、`gpt-image-2.5-flare`、`gpt-image-2.5-sunburst`
- `_pick()` 升级支持 `(display, value)` 二元组,显示 `display`、返回 `value`
- `text_to_image_flow()` / `image_to_image_flow()` 把 `_prompt("模型名", ...)` 改成 `_pick("选择模型", AVAILABLE_MODELS)`,用户不再需要粘贴
- 想加新模型直接在 `AVAILABLE_MODELS` 列表追加即可

## 长 prompt 多行输入修复(用户后续遇到)
- 现象:用户在 Windows 终端粘贴多段 prompt 时,每个 \n 都触发一次 input(),buffer 中剩余的段被后续 `_pick`/`_prompt` 自动消费,导致选完模型后"跳过"所有确认步骤,最后在 `n = int(...)` 那里抛 `ValueError`
- 修法:新增 `_read_prompt()`,支持三种模式自动识别:
  1) **单行 + 再回车结束**:粘贴一行 → 回车 → 再回车
  2) **多行 + `<<END>>` 结束**:粘贴多段 → 单独一行 `<<END>>` 结束
  3) **文件路径**:第一行直接是文件绝对路径,自动读整个文件
- `text_to_image_flow()` / `image_to_image_flow()` 都把 `prompt = _prompt(...)` 改成 `prompt = _read_prompt(...)`
- 4 个测试场景都通过(多行 <<END>>、单行空行、文件、多段空行收尾)

## 计时器重做(用户后续要求:进度条还看不到,要 ms 精度 + 美观)
- 之前用 rich.console.status 的 `\r` 同行刷新 + spinner,用户报告"还是看不到",推测:
  - 用户用系统 Python 跑,没装 rich
  - 旧版 Windows cmd 不一定支持 ANSI `\r` 覆盖
  - 日志复制时 `\r` 同行刷新被折叠
- 新实现:**完全去掉 rich,改成每 500ms 打印新行**(不用 `\r`)
  - 起始行:`⏳  <label>` + 注释行 `开始于 <HH:MM:SS.mmm>,调用 Thread-N (daemon 后台线程)`
  - 进度行:`spinner ⠋⠙⠹⠸⠼⠴⠦ + 墙钟 HH:MM:SS.mmm + +X.XXXs + (整数 ms)`
  - 完成行:`✓ 完成! 总耗时 X.XXXs (整数 ms)`
- 计时精度:`time.perf_counter()` 单调时钟 → ms 精度,毫秒向上滚动
- 颜色:ANSI 转义(`\033[36m` cyan / `\033[32m` green / `\033[90m` dim / `\033[1m` bold)
  - Windows 10+ / Windows Terminal / PowerShell 7+ 默认开启 VT,颜色会渲染
  - 旧版 cmd 颜色码会显示成乱码,但**文本内容一定可见**(只丢颜色,信息不丢)
- 实测 40.5s 生图 → 81 行进度记录(每 500ms 一行),节拍稳定,起止信息齐全